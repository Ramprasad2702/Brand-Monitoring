# BrandMonitor v3.0 — Python dependencies
# Install: pip install -r requirements.txt

# ── Core framework ────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ── HTTP + feeds (required) ───────────────────────
httpx>=0.27.0
feedparser>=6.0.11
aiofiles>=23.2.1
python-dotenv>=1.0.1
pydantic>=2.0.0

# ── Domain permutation ────────────────────────────
dnstwist>=20240101

# ── Visual phishing detection (optional but recommended)
# pip install playwright Pillow imagehash
# playwright install chromium
Pillow>=10.0.0
imagehash>=4.3.1

# ── NOTE: snscrape removed from PyPI
#    Social scraping uses httpx + Nitter/Reddit/HN directly (no extra package needed)
# ── NOTE: Scrapy replaced with httpx-based scraper (no C extensions needed)
